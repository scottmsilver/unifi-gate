{
  "name": "unifi-gate-auth-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for UniFi Gate Firebase JWT validation",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "jose": "^6.2.2"
  }
}
